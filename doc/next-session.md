# 다음 작업 재개 메모

- 작업 경로: C:/Work/Baseball. 문서 우선순위: doc/skills → 디자인 MD.
- POC 원본/산출물 분리, 구조·이식 문서 및 디자인 점검 완료. 기존 UI·Mines 룰·배당·정산 유지.
- 최신 작업: 카메라와 크기 맞추기 → 중앙 코스 스윙 → 25개 코스 확장 완료.
- 현재 배트: 약 86cm 물리적 길이, 최대 지름 6.7cm. 원근 투영으로 보이는 크기만 변하며, 5번째 열의 15% 확대는 제거했다.
- 공과 배트가 동일한 3D 카메라와 재생 시계를 사용한다. 공 지름은 7.3cm, 접촉은 굵은 배럴 옆면. 기존 화면 고정 크기·끝 단면 중심 접촉은 최신 방식으로 대체했다.
- 보이지 않는 어깨·팔꿈치·손목 연결로 스윙을 계산한다. 팔 길이는 고정하고 코스 높이에 따라 자세를 조정한다.
- 스윙 300ms(접촉 전 120ms + 후 180ms), 배트 잔상·페이드 없음. 안타 공은 페이드 없이 화면 상단 밖까지 날아간다. 하단 표시 마스크로 조작부를 보호한다.
- 검증: npm test 12개 통과. 실제 25개 라운드 HIT 22회·OUT 3회 및 정산 확인. WebGL/소프트웨어, 작은 화면, 동작 줄이기, 취소, 리사이즈, 그래픽 컨텍스트 중단 확인.
- 안타 타구: 매번 좌우 방향과 각도를 무작위로 선택해 화면 상단 밖까지 비행, 420ms. 배트는 300ms 유지하며 먼저 종료되어도 공은 계속 표시. 손잡이부터 배럴까지 나무 재질 통일.
- 실패 타격: 빗맞음(배트 윗면 접촉 후 짧게 튀어오름·360ms) 또는 헛스윙(비접촉 후 포수 방향 통과·180ms)을 무작위로 선택. 배트 300ms 및 OUT 정산 규칙 유지.
- 배트 외형: 매끄러운 배럴·테이퍼·노브, WebGL의 불규칙한 나뭇결과 얇은 코팅 반사 적용. 치수와 스윙 유지. 소프트웨어 렌더러는 간소한 곡면·음영 사용.
- 실패 팝업: 연출 완료 후 결과 문구와 화면을 1.2초 유지한 뒤 표시한다. 정상·빠른·동작 줄이기 공통이며 대기 중 입력 잠금을 유지한다.
- 화면 결과 문구: 실패는 OUT!으로 통일하고 HIT!/OUT!을 14cqw로 확대. 내부 실패 연출 2종과 1.2초 인지 시간 유지.
- 투수: 기존 4포즈를 7개 재생 구간으로 구성. 준비~릴리스 시간은 일반·빠른 모두 400ms(320+80ms), 실제 릴리스 완료 후 공·배트 시계 시작. 후속 동작·자세 회복 및 취소 초기화 구현.
- 투수 화염 오라: 현재 획득 배당 2/4/8/10x에 따라 4단계 강화. OUT·새 라운드에서 제거, 정산 보드에서는 유지. 동작 줄이기는 정적 표시. doc/pitcher-aura.md 참고.
- 사운드: 제공된 30초 BGM 반복, 클릭·투구·HIT·실패 2종·정산 합성 효과음 연결. 첫 입력 후 시작, 음표 버튼 전체 음소거·설정 저장, 탭 숨김 시 정지. doc/game-audio.md 참고.
- 세부 구현·한계·영상 참고 기록: doc/batting-swing.md. 배경은 2D이므로 카메라는 배경에 맞춘 근사값이며, 모션 캡처 재현은 아니다. 자연스러움에 대한 사용자 체감 확인이 다음 단계다.
- PixiBrown 템플릿 포크는 아직 없음. 실제 엔진 이식 미착수.
- 재실행: npm start. 주소 http://127.0.0.1:4173/
- 서버 실행 중 수정 반영: npm run build 후 새로고침.
